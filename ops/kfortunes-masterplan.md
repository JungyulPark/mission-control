# kfortunes 마스터플랜 (워크플로 검증판, 2026-07-29)

> 8에이전트 병렬 조사(5렌즈) + 통합설계 + 적대적검증 2종 결과. 49만 토큰, 121회 도구 사용.
> 원본: `tasks/wysw04eqk.output` (185KB) / run `wf_170978bf-026`
> **주의: 이 문서는 검증 결과이며, 사용자 승인 전이다. 승인 후 STATE의 정본이 된다.**

## 0. 한 줄 결론
**"kfortunes는 코드 배선 프로젝트가 아니다. 법무 게이트 1개 + 집필 프로젝트다."**
그리고 적대적 검증의 판정은 **"현 형태로는 실패한다"** — 아래 수정 없이는.

---

## 1. ⚠️ 뒤집힌 전제 (기존 문서 오류 — 반드시 정정)

### 오류 1: "115페이지 = AdSense 최소선 초과, 자산 풍부"
**틀렸다.** 그 115p의 실체는 `saju×51 + stars×50` = **동일 템플릿 반복**.
2026년 3월 코어 업데이트 이후 **scaled content abuse**가 명시적 집행 대상이고,
심사는 **사이트 전체 평균 품질**로 채점된다.
→ **그 페이지들은 심사 자산이 아니라 심사 부채다.** 잘 쓴 20편을 추가해도
템플릿 101p가 색인에 남아 있으면 평균을 끌어내린다.
→ 조치: 심사 전 **noindex + sitemap 제외**로 심사 범위에서 제거.
→ `ops/kfortunes-revival.md` 44행, `STATE.md`의 "최소선 초과" 표현 삭제 대상.

### 오류 2: "운세 주제라서 반려됐을 수 있다"
**아니다(호재).** Google Publisher Policies/Restrictions 어디에도 astrology·fortune
telling·divination 카테고리가 **없다**. 게다가 검색품질평가 가이드라인(2025-09-11판)은
astrology를 **"명백히 YMYL이 아닌"** 예시로 명시(fiction·folklore와 같은 묶음).
→ 자격증·기관인용 없이도 된다. 반려 원인은 **100% 콘텐츠 품질·구조**로 좁혀진다.
→ 단 **예외**: 질병·투자 **단정형 예측**으로 넘어가면 'Unreliable and harmful claims'
정책 위반으로 이동 → 오락 면책은 "있으면 좋은 것"이 아니라 **필수**.

### 오류 3: "재신청은 여러 번 해보며 배운다"
불가능. 반려 메일은 **구체적 사유를 주지 않는다**(한국 커뮤니티 다수 사례).
시행착오 학습이 성립하지 않으므로 **한 방에 맞춰야 한다**는 결론은 유지.

---

## 2. 🔴 적대적 검증이 찾은 치명적 결함 (계획을 죽이는 것)

| # | 결함 | 왜 치명적인가 | 수정 |
|---|---|---|---|
| **C1** | **`/result` 논리 모순** — "빌드타임 정적 본문" + "생년월일시 전량 클라이언트 계산"을 동시 요구. 단일 URL에 그 사용자 본문이 빌드타임에 들어갈 방법은 없다 | 런타임 JS로 가면 크롤러엔 빈 페이지 = **kstarmatch 반려사유 그대로 재현**. "view-source 본문 4,000자" 게이트가 물리적으로 달성 불가 | 결과를 유한 조합키 **K=(일간10 × 강한오행5 × 신강약3)=150개**로 환원해 `/r/{key}` **빌드타임 사전생성**. 입력폼은 클라이언트 계산 후 해당 URL로 **이동만**(생년월일시는 URL·서버 어디에도 안 남음 → 개인정보 무수집과 충돌 없음) |
| **C2** | **만세력 엔진 정확도 검증 태스크가 0개** | 한국 표준시는 **1954~1961 UTC+8:30**, 서머타임 1948~51·55~60·87~88. 진태양시 보정까지. → 55~60년생·87~88년생은 **시주가**, 절기 경계 출생자는 **일간 자체가** 틀린다. 일간이 틀리면 리포트·캐릭터·착지페이지 전부 남의 것. **60~70대가 사주 핵심 소비층** | M1↔M2 사이 **엔진 검증 게이트 신설**(반나절). 한국천문연구원 음양력 + 상용 만세력 2종 대비 **골든 테스트 40건**(1954~61 8건, 서머타임 12건, 절기경계 8건, 자시경계 8건, 윤달 4건). exit=40/40 일치 |
| **C3** | **AdSense 계정 상태 미조사** + kstarmatch 동거 | 계정이 '사이트 반려'가 아니라 **'계정 미승인/정지'**면 뭘 써도 무의미(1인 1계정, 우회 불가). 또 같은 계정에 매달린 **라이브 thin 사이트(kstarmatch)**가 심사를 끌어내릴 수 있는데 계획에 한 줄도 없었다 | **30분, 코드 0줄**: 대시보드에서 ①계정 상태 ②사이트 탭 도메인 목록·상태코드 ③결제 탭 수취인·세금정보·PIN — 스크린샷 확보. 신청 전 kstarmatch 연결 해제 또는 정리 |
| **C4** | **Cloudflare Pages ↔ 레포 ↔ 도메인 매핑 미확인** | STATE 19행: Pages가 `JungyulPark/kfortunes`에 연결. 그런데 그 레포 내용은 현재 kstarmatch. → **kfortunes 레포를 사주로 복원하는 순간 kstarmatch.com이 죽는다.** 라이브 사이트를 예고 없이 날림 | kfortunes는 **신규 Pages 프로젝트 + 신규 레포**로. 기존 프로젝트 재사용 금지 |
| **C5** | **집필 방식(AI 초안 가부)이 한 번도 규정 안 됨** | 포크 양쪽이 다 무너뜨린다. AI로 쓰면 "우리 조합은 115p 양산과 다르다"는 **유일한 정당화가 붕괴**(scaled content abuse 표적). 손집필이면 58,000자 = 29영업일 × 2,000자인데 **실증 base rate는 "6/6 하루 11커밋 후 47일 0건"** | M1 exit에 **집필방식 선언 1줄** 강제. ①전량 손집필→총량 20,000자로 축소 ②AI초안+전면재작성→재작성 검증기준을 숫자로 정의 ③혼합→블록별 구분 명시 |
| **C6** | **색인층 32p vs 집필예산 불일치** | `/element 5 + /sipseong 10` = 15p에 예산 미배정 → 페이지당 440~700자. **thin content를 sitemap+canonical 붙여 15장 재생산.** M2 게이트는 16편만 검사해 이 15장은 그냥 통과 | (A) 두 섹션을 색인층에서 빼고 필러 2편의 앵커 섹션으로 흡수(색인 32→17) **또는** (B) 페이지당 2,000자 추가 배정(+30,000자) |
| **C7** | **11/30 kill date가 자기 일정으로 도달 불가** | M3 만료 10/28 + 색인 2~4주 + 심사 2~4주 = **11/25~12/23**. kill 기준이 자기 분포 p10에 걸려 **정상 진행해도 자동 종료** | 마감을 Trends 실측 후 재설정하거나 해제 |
| **C8** | **애널리틱스 설치 태스크가 어디에도 없음** | 그런데 kill criteria가 "승인 후 3개월 월 1만 PV"에 의존. **스스로 정한 중단 기준을 측정할 수 없다** | M3에 **Cloudflare Web Analytics**(쿠키 없음·무료·개인정보 무수집 선언과 정합. GA4는 쿠키동의 문제로 배제) |
| **C9** | **Privacy가 승인 순간 거짓이 됨** | "개인정보 수집 안 함"으로 쓰는데 승인 후 AdSense는 광고 쿠키 사용 → **승인 직후 정책 위반 상태 진입** | 처음부터 **이중 서술**: 입력값은 브라우저에서만 계산·미전송 + 광고 게재를 위해 제3자(Google) 쿠키 사용 가능 + 옵트아웃 안내 |
| **C10** | **"광고 코드 0건" exit가 심사 개시와 충돌** | 소유권 확인 스니펫과 광고 유닛 노출은 **별개**인데 뭉뚱그림 → 게이트 지키면 심사가 시작 안 됨 | 두 줄 분리: (a)소유권 스니펫 배치 필수 (b)광고 유닛 렌더 0 + Auto ads OFF |
| **C11** | **승인 이후 운영계획 통째로 없음** | 특히 **바이럴 층(M5)이 무효 트래픽 리스크 최대 유입원** — 지인 공유→지인 클릭→계정 조치. 승인 직후 계정 상실 시나리오에 무방비 | `ops/kfortunes-ops.md`: 무효트래픽 금지선(지인에게 "광고 클릭 금지" 명시 요청, 본인 클릭 영구 금지), 광고배치 확정, 세운 연1회 갱신 담당, 정책위반 통지 대응 |

---

## 3. 🔵 순서 오류 — 이게 가장 중요한 수정

**원안:** M-1(법무·기관 노출) → M0.5(자산 감사)
**적대적 검증:** **거꾸로다.**

> 되돌릴 수 없는 행동은 되돌릴 수 있는 정보 수집 **뒤에** 와야 한다.
> 115p 감사·도메인 생존 확인은 몇 시간이면 끝나고 **노출이 0**인데,
> 그 결과가 "집필 58,000자냐 90,000자냐"를 결정하고 그게 **프로젝트를 할지 말지**를 결정한다.
> 지금 순서는 **프로젝트를 할 가치가 있는지 모르는 상태에서 기관에 노출**시킨다.

추가 순서 오류:
- **국립대/사립대 여부**가 open_question에 있는데 그게 M-1 질의문구의 전제. 근거법이 다르다
  (인사혁신처 표준지침 vs 사립학교법 §55 준용). → open_question이 아니라 **M-1 태스크 0번**.
- M0.5의 GSC·네이버 등록, DNS 확인 owner가 "kfortunes세션"인데 **전부 사용자 계정 로그인 필요** → 세션이 수행 불가.
- 색인 인프라 등록을 먼저 하면 Googlebot이 thin 115p를 **먼저 크롤·색인**한다 → noindex를 M3까지 미루면 안 됨.

---

## 4. ⚖️ 법무 게이트의 진짜 딜레마 (해결 안 됨)

- **물어보면 노출되고, 안 물어보면 무허가.** 겸직허가 신청 행위 자체가 "의대 교수가 사주 사이트
  운영"을 기관에 공식 노출시킨다.
- "의대 교수의 사주 사이트가 품위유지의무에 저촉되는가" — **선례를 찾지 못했다(confidence low)**.
- 원안 exit는 `가능/조건부/불가` 3값인데, **한국 대학 인사팀의 최빈 응답인
  "정식 신청 전 서면 답변 불가"가 어디에도 없다.** 게다가 8월은 여름휴가 구간.
  → 정상 응답해도 게이트 미충족 → kill 발화. **질의 서식 문제로 프로젝트가 자동 종료된다.**
- **수정: exit를 4값으로.** 네 번째("정식 신청 전 답변 불가")는 kill이 아니라 **분기** —
  기관 대신 **세무사·노무사 유료 상담 + 동종 사례(교원 유튜브 수익 신고)**로 판단 근거를 대체하고,
  "연 수익 X원 도달 시 정식 신청"이라는 **조건부 트리거**로 전환.

**이 게이트가 '불가'로 나오면 프로젝트 전면 중단이다.** 성공하는 순간(수익 발생) 본업 징계
리스크가 켜지는 구조라, **승인 후에 알게 되는 것이 최악**이다.

---

## 5. 수정된 실행 순서 (제안)

```
M0.5' 무노출 정보수집 (사용자, 2~3시간, 리스크 0)   ← 지금 여기
   ├ AdSense 대시보드 3종 스크린샷(계정상태/사이트목록/결제정보)   [C3]
   ├ Cloudflare Pages ↔ 레포 ↔ 도메인 매핑 표               [C4]
   ├ kfortunes.com DNS/호스팅 생존 여부                     
   ├ 48edd60 115p 고유 단어수 실측 (git show, 복원 불필요)   [오류1 정량화]
   └ 소속: 국립대/사립대 (10초)                             [M-1 전제]
        ↓  결과가 "할 가치 있음"이면
M-1'  법무 게이트 (4값 exit, 병렬 트랙)                      [§4]
        ↓
M0'   문서 정정 (관제탑) — "최소선 초과" 표현 제거 등
M1'   아키텍처 동결: /r/{key} 150p 사전생성 + 집필방식 선언 + 색인층 확정  [C1 C5 C6]
M1.5' 엔진 검증 게이트 — 골든 테스트 40/40                   [C2]
M2'   Tier1 집필 (총량은 M0.5' 실측 후 확정)
M3'   배선·렌더러·정적페이지·성능 + Analytics + Privacy 이중서술  [C8 C9]
M4'   색인 확보 → 신청 (소유권 스니펫 O / 광고유닛 0)         [C10]
M5'   승인 후에만: 운영수칙 먼저 → 바이럴                     [C11]
```

## 6. Kill Criteria (원안 유지, 날짜만 재설정 대상)
- 겸직 '불가' 또는 품위유지 저촉 → **즉시 전면 중단**
- 세무 상담이 '사업자등록 없이 수취 위법 소지' → **즉시 중단**
- M0.5' 감사 결과 색인 가능 품질이 5편 미만인데 6주 집필 약속 불가 → 중단
- 집필 진척이 기한 절반에 총량 절반 미만 → 중단 (6/6~7/29 **7주 무진척 전력** 반영)
- AdSense 승인 실패 시 → 자원 전량 **n8n 안정화·유튜브 재개**로 이전
  (유튜브는 병목이 1곳, kfortunes는 5곳이고 그중 2개는 오너 통제 밖)
- 상시: 결제·실시간GPT·백엔드 부활이 재등장하거나 M4 전에 궁합·토정비결 확장이 제안되면 → 루프 정지

## 7. 미해결 (조사로 못 닫은 것)
- 의대 교수 사주 사이트의 품위유지의무 저촉 여부 — **선례 없음**
- Google Trends 실측 없음 — "연초 피크" 가설이 마감 전체를 떠받치는데 정량 근거 없음(30분이면 검증)
- 한국이 AdSense 6개월 도메인 연령 요구국인지 — 1차 출처 미확보
- "운세는 금지도 제한도 아님"을 Google **원문 fetch로 직접 확인 실패**(403, 검색요약 경유,
  confidence medium). **이 판단이 틀리면 계획 전체가 무너진다**
- 필명 저자로 E-E-A-T가 성립하는지는 사전 검증 불가 — 실명·직함 봉인이 가장 강한 신뢰신호를 막는다
